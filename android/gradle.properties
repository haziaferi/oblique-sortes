org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# The app uses no AndroidX at all — platform views, platform theme — but the
# flag is deprecated in AGP 9, so it is simply left at its default.
android.nonTransitiveRClass=true
